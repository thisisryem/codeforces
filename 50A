    #include <iostream>
    using namespace std;
     
    int main() {
        int M,N;
        cin >> M >> N;
        if(M>=1 && N<=16)
        {
            int dom = (M*N)/2;
            cout << dom << endl;
        }
        return 0;
    }
